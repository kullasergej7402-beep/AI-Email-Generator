# Фаза 6 — Полировка: ошибки, адаптив, тесты, Docker/CI

**Предпосылки:** пройдены фазы 0–5 (основной функционал готов).
**После фазы:** нет белых экранов, отлажен адаптив, есть тесты и контейнеризация. Commit после каждого промпта.
**Совет:** если поджимает время — 6.3 и 6.4 можно сделать в последнюю очередь, ядро задания они не блокируют.

---

## Промпт 6.1 — Error handling (никаких белых экранов)

```
Опираясь на CLAUDE.md, закрой обработку ошибок по всему приложению.

1. Добавь app/error.tsx (глобальный error boundary) и app/not-found.tsx — дружелюбные экраны с кнопкой "На главную" / "Повторить".
2. Добавь error.tsx и loading.tsx в сегменты /dashboard, /profile, /pricing.
3. Убедись, что все fetch/server actions ловят ошибки и возвращают понятный текст; на клиенте показываем toast или inline-alert, а не падаем.
4. Добавь утилиту нормализации ошибок (Supabase/AI/валидация -> единый человекочитаемый месседж).
5. Проверь сценарии: нет сети, AI кинул ошибку (включи флаг в mock), невалидный ввод, доступ к чужим данным (RLS). Везде — корректный UX, белого экрана быть не должно.

Опиши, как ты протестировал каждую ветку.
```

---

## Промпт 6.2 — Responsive-полировка + анимации

```
Опираясь на CLAUDE.md, пройди по всем страницам (landing, login, signup, dashboard, pricing, profile) и доведи адаптив:

- breakpoints: mobile (~375), tablet (~768), desktop (~1280)
- бургер-меню, читаемые шрифты, отсутствие горизонтального скролла, тач-таргеты >= 44px
- единые отступы и сетка
- аккуратные framer-motion анимации (появление секций, hover на карточках, плавная отрисовка результата генерации) — сдержанно, без перегруза

Сделай чек-лист пройденных страниц с заметками, что поправил.
```

---

## Промпт 6.3 — Тесты

```
Опираясь на CLAUDE.md, добавь тесты.

1. Vitest + Testing Library для unit/компонентов:
   - mock-провайдер (детерминизм, ветка ошибки)
   - утилита нормализации ошибок
   - валидация zod-схем форм
   - рендер ключевых компонентов (форма генерации, карточка тарифа)
2. Playwright e2e — хотя бы один happy-path: открыть лендинг -> signup -> dashboard -> сгенерировать письмо (на mock) -> увидеть результат.
3. Добавь npm-скрипты test, test:e2e и инструкцию в README.

Сделай так, чтобы e2e гонялись на AI_PROVIDER=mock без реальных ключей.
```

---

## Промпт 6.4 — Docker + CI/CD

```
Опираясь на CLAUDE.md, добавь контейнеризацию и CI.

1. Dockerfile (multi-stage, output: 'standalone' в next.config) + .dockerignore. Образ должен стартовать `npm start`.
2. docker-compose.yml для локального запуска (app + переменные окружения).
3. GitHub Actions .github/workflows/ci.yml: на push/PR — install, lint, typecheck, unit-тесты, build. Используй AI_PROVIDER=mock и фиктивные env, чтобы CI не требовал секретов.
4. Обнови README разделами "Docker" и "CI/CD".

Проверь, что образ собирается и контейнер поднимается локально.
```
