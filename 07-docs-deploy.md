# Фаза 7 — Документация и деплой

**Предпосылки:** проект собран и протестирован.
**После фазы:** готовы README, AI Development Report, проект задеплоен на Vercel. Это финальная часть сдачи.

> ⚠️ **GitHub-репозиторий, реальный Supabase-проект и кнопку Deploy нужно сделать с твоего аккаунта** — Claude Code подготовит весь код и инструкции, но запушить и задеплоить должен ты сам.

---

## Промпт 7.1 — README

```
Опираясь на CLAUDE.md и итоговый код, напиши финальный README.md (на русском, чисто и структурно):

- Краткое описание проекта и фич
- Стек технологий
- Структура проекта (дерево с пояснениями ключевых папок)
- Локальный запуск: предпосылки, установка, настройка Supabase (миграция), переменные окружения (.env.example), запуск dev
- Переключение mock <-> реальная LLM (AI_PROVIDER)
- Скрипты (dev, build, test, test:e2e, lint)
- Docker-запуск
- Деплой на Vercel (кратко)
- Раздел "Принятые решения" — почему именно этот стек, как устроен AI-слой, как устроена монетизация, как сделана обработка ошибок

Без воды, по делу. Покажи готовый README.
```

---

## Промпт 7.2 — AI Development Report

> Это **ключевая часть сдачи**. Пока проходил фазы — у тебя должны быть выписаны реальные косяки и решения. Скорми их Claude Code, чтобы отчёт был честным, а не шаблонным.

```
Опираясь на CLAUDE.md и историю нашей работы, составь AI Development Report (docs/AI_DEVELOPMENT_REPORT.md, на русском) — это ключевая часть сдачи.

Опиши честно:
1. Какие AI-инструменты использовались (Claude Code) и какие модели.
2. Как строился процесс: пофазно, plan mode, коммиты между шагами, как ловили и чинили ошибки.
3. 10-20 самых важных промптов, которые реально двигали разработку (можно взять ключевые из этого процесса, слегка причесав). Сгруппируй по фазам.
4. Что бы улучшил при наличии большего времени: реальный Stripe, стриминг ответа LLM, больше провайдеров, шаблоны писем, rate-limiting на бэке, расширенные e2e, observability.

Сделай документ конкретным и правдоподобным, отражающим именно этот проект. Вот мои заметки по ходу разработки, используй их: [ВСТАВЬ СВОИ ЗАМЕТКИ]
```

---

## Промпт 7.3 — Подготовка к деплою на Vercel

```
Опираясь на CLAUDE.md, подготовь проект к продакшен-деплою на Vercel.

1. Проверь next.config (нужные image domains, output standalone не ломает Vercel).
2. Убедись, что все секреты читаются из env и нигде не захардкожены.
3. Составь точный список переменных окружения, которые нужно завести в Vercel (Supabase URL/anon key, ANTHROPIC_API_KEY, AI_PROVIDER), с пометкой какие обязательны.
4. Дай мне пошаговую инструкцию: импорт репозитория в Vercel -> добавить env -> deploy; и что выставить в Supabase (Auth redirect URLs на прод-домен).
5. Прогони финальный `npm run build` локально и почини любые предупреждения/ошибки.

Выведи финальный чек-лист "перед сдачей": рабочий URL открывается, signup/login работают, генерация на mock или реальной модели отвечает, нет белых экранов, адаптив ок.
```
